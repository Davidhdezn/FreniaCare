<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="davidhdezn.app.freniacare">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NavegationDraw"
            android:label="@string/title_activity_navegation_draw"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".Fragments.MenuPrincipal" />
        <activity android:name=".Cargando" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login"
            android:theme="@style/SinBar" />
        <activity android:name=".RegistroUsuario" />
        <activity android:name=".nav_header_navegation_draw" />
    </application>

</manifest>